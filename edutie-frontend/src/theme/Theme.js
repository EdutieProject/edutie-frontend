import { createTheme } from '@mui/material/styles';


const theme = createTheme({
  palette: {
    primary: {
      main: "#1FB45E"
    },
    white: {
      main: "#FFFFFF"
    },
    black: {
      main: "#000000"
    }
  },
});

export default theme;